{
  "name": "skillx",
  "private": true,
  "packageManager": "npm@YOUR_VERSION",
  "description": "SkillX - AI-powered skill barter and mentorship platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}