{
  "name": "skillx",
  "private": true,
  "packageManager": "npm@11.6.1",
  "description": "SkillX - AI-powered skill barter and mentorship platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "build:api": "turbo build --filter=@skillx/api",
    "db:generate": "turbo db:generate",
    "postinstall": "npm run db:generate",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}